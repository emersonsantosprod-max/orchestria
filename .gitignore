# Python environment
venv/
venv_win/
__pycache__/
*.py[cod]

# PyInstaller artifacts
build/
dist/

# setuptools editable-install metadata
*.egg-info/

# Testing cache
.pytest_cache/
.ruff_cache/

# Runtime logs
logs/

# Runtime data — entire directory excluded, no exceptions
/data/

# Baseline outputs — regenerados sob demanda; ver scripts/diff_medicao.py
baseline/

# assets/ — bundled source-of-truth (distribuicao xlsx). Versionado.
# Nada a ignorar aqui por padrão.

# Claude Code local settings
.claude/settings.local.json

# Session scratchpads (ephemeral, not committed)
.claude/sessions/*.tmp

# Design files
design/
